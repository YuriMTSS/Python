# Passo 7: Exportando a nova base de dados


# OBS: 
    # Se for para editar a base de dados, primeiro feche ela e depois coloque o MESMO NOME no campo das aspas abaixo
    # Se for criar uma nova base de dados, coloque um nome diferente
tabela.toExcel("") # Nome da base de dados

# Se a tabela nova tiver os numeros na primaira coluna
    # 0
    # 1
    # 2
    # 3
    # .
    # .
    # .
# Use o seguinte comando
tabela.to_excel("", index = False) # Nome da tabela e o index falso (coluna dos numeros)

navegador.quit() # Para terminar, fechamos o navegador
