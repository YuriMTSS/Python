# Quando usa o Selenium e quando usa o Pyautogui?
# Pode usar os 2 juntos sem problemas
# Mas quando é uma automação 100% na internet, use o Selenium
# Quando é um sistema da empresa, que não necessita da internet, use o Pyautogui

============================================================================================================================
# Passo 1: Entrar no Google.

from selenium import webdrive # Cria o navegador
from selenium.webdriver.common.by import By # localiza elementos ou itens de um site
from selenium.webdriver.common.keys import Keys # Permite clicar teclas do teclado

navegador = webdriver.chrome()
navegador.get("https://www.google.com/")


