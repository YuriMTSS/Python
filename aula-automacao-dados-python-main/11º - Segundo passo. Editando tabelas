# Passo 2: Tratamento de dados
# Entender as informações que estão disponiveis para voce
# Corrigir o que é inútil na tabela


tabela = tabela.drop(nome, eixo) # nome = nome da coluna ("Unmaed"), eixo = posição da coluna na linha (axis = 1)


# Estou mandando excluir a tabela "tal" no eixo "tal"
# Ex: 
    #     conta | Unmaed | calçados
    #     -------------------------
    # 0    12   |  20    | 30
    # 1    22   |  89,21 | 21
 
# tabela = tabela.drop("Unmaed", axis 1) ------- Nome da tabela e o eixo dela na linha
# axis = 0 -> Linha
# axis = 1 -> Coluna
# Se for deletar N colunas de uma vez só: tabela = tabela.drop(["coluna 1", "coluna 2", "coluna 3"], axis = 1)
