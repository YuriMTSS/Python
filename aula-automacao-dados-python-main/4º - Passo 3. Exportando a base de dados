# Passo 3: Exportar/Fazer download da base de dados

# Sem segredos, usando o que aprendeu no passo 2 é só clicar com o mouse nas posições precisas várias vezes para fazer o que quer.
# No caso do curso foi usado o Google Driver para o curso, portanto, os 3 cliques foram

pyautogi.click() # clicar no arquivo 1x
pyautogi.click() # clicar nos 3 pontinhos
pyautogi.click() # clicar em fazer download
time.sleep(5) # tempo suficiente para fazer o download (dependendo da velocidade do seu computador e hardware)


