# A biblioteca pandas é exclusiva para analise da dados, todo tipo de dado ou planilha ele consegue ler
# O comando "as pd" é um apelido, sempre que quiser amenizar uma palavra muito grande use "as 'apelido'"

# OBS: Se você estiver usando o Jupyter o pandas vem instalado automaticamente, se for outro editor de códigos
# Abra o terminal e digite: "pip install pandas" e aguarde o download terminar
# Junto do pandas precisa ter as seguintes bibliotecas: "pip install numpy", "pip install onpenpyxl"
# Se estiver usando o Jupyter, os 3 pacotes á vem instalados
====================================================================================================================================
# Passo 4: Importar a base de dados para o Python

# Para resolver o passo 4 precisaremos ler a base de dados
import pandas as pd

# Armazenaremos o arquivo na variavel tabela, se for outra extensão use a extenção
tabela = pd_read.excel(r"") # Entre aspas use o caminho TOTAL do arquivo incluindo seu nome no final

# O 'r' informa que o CAMINHO DE ARQUIVO não terá caracteres especiais (\n, \t, etc)
# OBS: Dependendo do sistema operacional usado o caminho é diferente, mas funciona DA MESMA FORMA

display(tabela) # Se estiver usando o Jupyter é 'display()', se for outro editor de códigos é o 'print(tabela)'


