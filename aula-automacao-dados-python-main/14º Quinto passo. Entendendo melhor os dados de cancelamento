# Para finalizar precisamos de um pacote chamado plotly
# pip install plotly
==============================================================================================================================
# Passo 5: Análise mais completa -> Entender a causa dos cancelamentos/possíveis soluções

import plotly.express as px


# Criando o gráfico
# Para cada coluna na minha base de dados eu quero um gráfico

for coluna in tabela.colmuns:
    grafico = px.histogram(tabela, x = coluna, color = "Churn")    

# Se fossem linhas
for linhas in tabela.rows:
    ||||||||||||||||||||||||||||||||||||||||||||||||


# grafico = px.histogram(tabela, x = "", color = "Churn") # Tipo de grafico - Tabela - Coluna - Cor
# grafico = px.histogram(tabela, x = "", color_discrete_sequence = ["blue","green"])

# Exibindo o gráfico
    grafico.show()
